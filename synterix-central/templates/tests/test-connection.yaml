apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "synterix-central.fullname" . }}-test-connection"
  labels:
    {{- include "synterix-central.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "synterix-central.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
